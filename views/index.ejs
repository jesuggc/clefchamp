<!DOCTYPE html>
<html lang="es">
  <%-include('partials/head') %>
  <body class="colorNuevo5">
    <%-include('partials/header') %>
    <% if(!user) { %>
    <div class="d-flex align-items-center justify-content-center vh-90" id="mainContent">
      <div>
        <h1 class="text-center" style="font-size: 14em;color: #1E0126;">Clefchamp</h1>
        <h2 class="text-center"> Plataforma de aprendizaje musical mediante gamificación</h2>
        <div class="d-flex justify-content-center mt-5">
          <a href="/play/pruebaNivel"><button class="cancelButton me-5">Prueba de nivel</button></a>
          <a href="/loading?redirect=/moreInformation"><button class="acceptButton">Saber más</button></a>
        </div>
      </div>
    </div>
    <% } else { %>  
    <div class="bg-secondary rightRounded  row" style="width:40%">
      <div class="col">
        <div class="rightRounded justify-content-end mt-3 pt-2 pb-2 pe-4 d-flex " style="width:60%; background-color: white;">
          <h3><%=locals.user.name%></h3>
        </div>
        <div class="d-flex justify-content-end pt-3 text-white" style="width:75%;">
          <span><%=locals.user.experience%>/<%=locals.user.experience + locals.user.experienceToNext%></span>
        </div>
        <div class="progress" style="border-radius: 0 2em 2em 0 ; width:85%;height:3em" role="progressbar" aria-label="Success striped example" aria-valuenow="<%=locals.user.experience+30%>" aria-valuemin="0" aria-valuemax="<%=locals.user.experience + locals.user.experienceToNext%>">
          <div id="levelBar" class="progress-bar bg-success slider overflow-visible text-dark" style="width: <%=((locals.user.experience)/(locals.user.experience + locals.user.experienceToNext))*100%>%" >
            <h3 id="levelText" class="ps-5 pt-1">Nivel <span><%=locals.user.level%></span></h3>
          </div>
        </div>
      </div>
      <div class="col-3 d-flex align-items-center justify-content-center">
        <img src="/images/<%=locals.user.icon%>" class="rounded-circle mb-1 mt-1 me-2" style="max-height: 10em;">
      </div>

    </div>
      
    <div class="d-flex align-items-center justify-content-center vh-90" id="mainContent">
      <div>
        <h2 class="text-center"> Plataforma de aprendizaje musical mediante gamificación</h2>
        <div class="d-flex justify-content-center mt-5">
          <a href="/"><button class="cancelButton me-5">Jugar</button></a>
        </div>
        <audio src="/audio/FurElise.mp3" controls>
          Your browser does not support the <code>audio</code> element.
        </audio>
      </div>
    </div>
    <% } %>
    <%-include('partials/footer') %>
    <script>
      // Configurar la recarga cada 5 segundos (5000 ms)
      // setInterval(() => {
      //   window.location.reload();
      // }, 1500);
    </script>
  </body>
</html>