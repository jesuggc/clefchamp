<!DOCTYPE html>
<html lang="es">
  <%-include('partials/head')%>
  <body style="background-color: #ffa919;">
    <%-include('partials/headerLogo')%>
    <div class="d-flex align-items-center">
      <div class="row justify-content-center text-center w-100">
          <h1 class="pb-4">Por las barbas de Johannes Brahms</h1>
          <div class="col">
            <img src="/images/error500.jpg" style="max-width: 40em; max-height: 30em;">
          </div>
          <% if(message) { %>
            <div class="col text-start">
              <p><%= message %></h1>
              <pre><%= error.stack %></pre>
              <h5><%= error.status %></h2>
            </div>
          <% } %>
          <h1 class="pt-4">Parece que la música ha dejado de sonar...</h1>
        </div>
    </div>
  </body>
</html>

